# Retina CAD Architecture Overview

Use [`architecture-overview-diagram.svg`](./architecture-overview-diagram.svg) when you need the actual standalone diagram.

Open [`architecture-overview-guide.html`](./architecture-overview-guide.html) for the longer advisor-facing explainer around that diagram.

Use this when the goal is to explain the full system at a higher level than the existing module-specific diagrams:

- raw OCTA + clinical inputs
- layer-specific representation learning
- multimodal fusion
- prediction, calibration, explainability

It is intentionally simpler than the deeper architecture docs and keeps implemented work separate from downstream planned evaluation.
